<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">HOME IMPROVEMENT</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% if current_user %>
        <li><%= link_to 'HOME', projects_path, :class => "nav-link" %></li>


        <li><%= link_to "MY PROJECTS", profile_path(current_user.id), :class => "nav-link" %></li>

        <% if current_user.admin %>
          <li><%= link_to "USERS", users_path, :class => "nav-link" %></li>
        <% end %>
       <% end %>
      </ul>
      <button id="new-project-btn" class="btn navbar-btn">NEW PROJECT</button>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>

        <li>
        <button id="sign-in-btn" class="btn navbar-btn">SIGN OUT</button>

        </li>


        <% else %>
        <li>
        <button id="sign-in-btn" class="btn navbar-btn">SIGN UP</button>

        </li>
        <li>
        <button id="sign-in-btn" class="btn navbar-btn">SIGN IN</button>

        </li>
        <% end %>

      </ul>

    </div>
  </div>
</nav>



