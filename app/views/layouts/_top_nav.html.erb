<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">HOME IMPROVEMENT</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% if current_user %>
        <li><%= link_to 'HOME', projects_path, :class => "nav-link" %></li>
        <li><%= link_to "MY PROJECTS", profile_path(current_user.id), :class => "nav-link" %></li>
        <% if current_user.admin && false %>
        <li><%= link_to "USERS", users_path, :class => "nav-link" %></li>
        <% end %>
        <li>
          <%= link_to "NEW PROJECT", new_project_path, :class => "btn navbar-btn", :id => "new-project-btn" %>
        </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>

        <li>
          <%= link_to "SIGN OUT", destroy_user_session_path, method: :get, :class => "btn navbar-btn", :id => "sign-in-btn"%>
        </li>
        <% else %>
        <li>
          <%= link_to "SIGN UP", new_registration_path(resource_name), :class => "btn navbar-btn", :id => "sign-in-btn" %>
        </li>
        <li>
         <%= link_to "SIGN IN", new_session_path(resource_name), :class => "btn navbar-btn", :id => "sign-in-btn" %>
       </li>
       <% end %>
     </ul>
   </div>
 </div>
</nav>



